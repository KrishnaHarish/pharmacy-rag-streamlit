# Application Configuration
# This app uses GitHub-hosted models - no API keys required
# Models are accessed through GitHub's infrastructure when running in GitHub Codespaces or similar environments
